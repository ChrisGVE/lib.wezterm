return {
	default = {
		ROOT = { "tests/unit" },
		lpath = "./plugin/?.lua;./tests/unit/?.lua",
		verbose = true,
		-- Enable coverage in CI, disable locally for easier development
		coverage = os.getenv("CI") == "true",
	},
}
